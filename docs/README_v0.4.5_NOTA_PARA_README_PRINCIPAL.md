## v0.4.5 - Google Search Console

La version v0.4.5 incorpora el archivo de verificacion de Google Search Console en la raiz publica del proyecto:

```txt
googlebf14d2205ce8e958.html
```

URL publica esperada:

```txt
https://levante-tls.pages.dev/googlebf14d2205ce8e958.html
```

Tras desplegar en Cloudflare Pages, se debe volver a Search Console y pulsar `VERIFICAR`. Despues se envia el sitemap:

```txt
https://levante-tls.pages.dev/sitemap.xml
```

Y se solicita indexacion para las paginas principales.
