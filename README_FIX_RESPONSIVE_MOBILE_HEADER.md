# Corrección responsive móvil - Header y hero

Se ha añadido un bloque en `assets/css/responsive.css`:

```plaintext
MOBILE FIRST FIX - HEADER / IDIOMAS / HERO
```

Corrige:
- overflow horizontal en móvil,
- tamaño de logo y marca,
- botones de contacto del header,
- selector de idiomas con scroll horizontal interno,
- escudo y valores del header,
- título principal del hero en móvil.

Después de subir a GitHub Pages, haz recarga dura o borra caché del móvil.
